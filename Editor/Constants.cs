using System;

namespace Packages.AutoDarkMode
{
    public static class Constants
    {
        public const string Name = "Auto Dark Mode";
        public static readonly TimeSpan AutoFetchInterval = TimeSpan.FromDays(1);
    }
}